import type { RawElectionInput } from '@/types/election'

const buildCandidateRows = (
  electionId: string,
  electionName: string,
  regionName: string,
  adminCode: string,
  adminLevel: 'province' | 'city' | 'town',
  turnoutRate: number,
  leadingParty: string,
  leadingShare: number,
  challengerParty: string,
  challengerShare: number,
) => {
  const totalValidVotes = 100000
  const totalVotes = Math.round(totalValidVotes / Math.max(0.1, turnoutRate / 100))

  return [
    {
      electionId,
      electionName,
      electionType: adminLevel === 'province' ? 'local-governor' : 'mock',
      electionDate: '2026-06-03',
      regionName,
      adminCode,
      adminLevel,
      candidateName: `${leadingParty} 후보`,
      partyName: leadingParty,
      voteCount: Math.round(totalValidVotes * (leadingShare / 100)),
      voteRate: leadingShare,
      totalValidVotes,
      totalVotes,
      turnoutRate,
      sourceDate: '2026-06-04',
    },
    {
      electionId,
      electionName,
      electionType: adminLevel === 'province' ? 'local-governor' : 'mock',
      electionDate: '2026-06-03',
      regionName,
      adminCode,
      adminLevel,
      candidateName: `${challengerParty} 후보`,
      partyName: challengerParty,
      voteCount: Math.round(totalValidVotes * (challengerShare / 100)),
      voteRate: challengerShare,
      totalValidVotes,
      totalVotes,
      turnoutRate,
      sourceDate: '2026-06-04',
    },
  ] satisfies RawElectionInput[]
}

export const mockElectionResults: RawElectionInput[] = [
  ...buildCandidateRows(
    '2026-local-governor',
    '2026 지방선거 광역단체장',
    '서울특별시',
    '11',
    'province',
    61.4,
    '개혁연합',
    49.3,
    '미래시민',
    43.1,
  ),
  ...buildCandidateRows(
    '2026-local-governor',
    '2026 지방선거 광역단체장',
    '경기도',
    '41',
    'province',
    60.1,
    '개혁연합',
    48.1,
    '미래시민',
    43.3,
  ),
  ...buildCandidateRows(
    '2026-local-mayor',
    '2026 지방선거 기초단체장',
    '종로구',
    '11110',
    'city',
    63.1,
    '개혁연합',
    52.1,
    '미래시민',
    37.4,
  ),
  ...buildCandidateRows(
    '2026-local-mayor',
    '2026 지방선거 기초단체장',
    '해운대구',
    '26350',
    'city',
    57.2,
    '미래시민',
    48.5,
    '개혁연합',
    40.7,
  ),
  ...buildCandidateRows(
    '2026-local-town',
    '2026 지방선거 동 단위 샘플',
    '청운효자동',
    '11110515',
    'town',
    68.1,
    '개혁연합',
    51.8,
    '미래시민',
    42.0,
  ),
]
